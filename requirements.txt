# ============================================================
# LLM-RL-Pipeline 依赖清单
# 支持 Qwen3.5-9B SFT + GSPO RL 训练 + GGUF 量化导出
# 硬件要求: 4× A5000 (24GB each, 96GB total)
# ============================================================

# --- Core DL Framework ---
torch>=2.4.0
torchvision>=0.19.0
torchaudio>=2.4.0

# --- Transformers & Model Libraries ---
transformers>=4.46.0
accelerate>=1.0.0
peft>=0.13.0
bitsandbytes>=0.44.0
trl>=0.12.0

# --- Unsloth (QLoRA 加速) ---
unsloth>=2024.12.0
unsloth_zoo>=2024.12.0

# --- Data Processing ---
datasets>=3.1.0
pandas>=2.2.0
numpy>=2.0.0
pyarrow>=17.0.0

# --- HuggingFace Hub ---
huggingface_hub>=0.26.0

# --- Reward Function Support ---
sympy>=1.13.0          # 数学表达式解析
antlr4-python3-runtime>=4.13.0

# --- GGUF Export ---
llama-cpp-python>=0.3.0

# --- Training Utilities ---
wandb>=0.18.0           # 实验追踪 (可选)
tensorboard>=2.18.0     # 训练可视化
tqdm>=4.67.0
safetensors>=0.4.0

# --- Jupyter / Dev ---
jupyter>=1.1.0
ipykernel>=6.29.0
ipywidgets>=8.1.0

# --- Testing ---
pytest>=8.3.0
pytest-cov>=6.0.0